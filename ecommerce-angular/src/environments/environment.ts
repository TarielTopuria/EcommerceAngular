export const environment = {
  apiUrl: 'https://fakestoreapi.com',
  // FakeStoreAPI does not persist PUT/DELETE/POST across requests.
  // Enable localStorage mutations so the UI behaves like a real backend.
  persistLocalMutations: true,
};
